@{
    ViewBag.Title = "Access Denied";
}

<h2>Access Denied</h2>
<p>You do not have permission to view this page.</p>

@if (!string.IsNullOrEmpty(ViewBag.ReturnUrl))
{
    <p>Return to the <a href="@ViewBag.ReturnUrl">previous page</a>.</p>
}
